{
  "name": "post-it-library-repo",
  "private": true,
  "workspaces": ["packages/*", "demo"],
  "scripts": {
    "build": "npm run build --workspace=post-it-library",
    "dev": "npm run dev --workspace=demo",
    "preview": "npm run preview --workspace=demo"
  }
}
